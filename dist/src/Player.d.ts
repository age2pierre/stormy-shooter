import { ReactNode } from 'react';
export declare function Player({ position, children, }: {
    position: [x: number, y: number];
    children?: ReactNode;
}): import("react/jsx-runtime").JSX.Element;
